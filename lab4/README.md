Лабораторна робота №4
Тема: Просунутий аналіз даних, агрегація та робота зі зведеними таблицями.  
Виконав студент групи ФБ-46, Ільченко Владислав.

Опис виконання:
1. Feature Engineering: Проведено створення нових аналітичних ознак на основі існуючих даних для глибшого розуміння характеристик об'єктів.
2. Агрегація даних: Використано методи групування для обчислення комплексних статистичних показників (середнє, мінімум, максимум) за різними категоріями.
3. Зведені таблиці: Побудовано багатовимірні структури даних для аналізу взаємозв'язків між походженням об'єктів та їх ціновими сегментами.
4. Складна фільтрація: Реалізовано багаторівневий відбір даних за декількома технічними умовами одночасно.

Детальний опис програмного коду:
Програма написана мовою Python у середовищі Jupyter Notebook із використанням наступних модулів:

Блок підготовки даних:
Використано бібліотеку Pandas для імпорту датасету та бібліотеку NumPy для проведення математичних розрахунків. На цьому етапі було проведено фінальне очищення даних: заповнено пропуски в колонках ціни, потужності та ваги за допомогою методу `fillna()`, що дозволило уникнути помилок при агрегації.

Блок аналітичних операцій:
1. Створення ознак: Розрахувано нову метрику — питому потужність (`Power_Weight_Ratio`), як відношення кінських сил до ваги авто.
2. Квантильний поділ: За допомогою функції `pd.qcut` об'єкти були автоматично розбиті на три рівні групи: *Budget*, *Standard* та *Premium*.
3. Групування (GroupBy): Використано метод `groupby` разом із функцією `agg()`. Це дозволило отримати розширену статистику (середню ціну, розмах цін) для кожного типу кузова.
4. Зведені таблиці (Pivot Tables): Застосовано функцію `pivot_table` для створення матриці розподілу, де рядками є країна походження (`Origin`), а стовпцями — рівень ціни.
5. Складна фільтрація: Використано комбіновану фільтрацію для відбору моделей, тех. характеристики яких перевищують середньостатистичні показники по всьому датасету.


Висновок:
У ході роботи було опановано інструменти глибокого аналізу даних засобами Pandas. Встановлено, що використання зведених таблиць та групування дозволяє швидко ідентифікувати закономірності в структурі ринку.
